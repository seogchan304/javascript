// const a = window.prompt("문자 1개를 입력하세요");

// const string = window.prompt("문자열을 입력하세요");
// const word = window.prompt("문자를 입력하세요");
// if (string.includes(word)) {
//   window.console.log("문자열에는 문자가 포함되어 있습니다");
// } else {
//   window.console.log("문자열에는 문자가 포함되어있지 않습니다");
// }

// function quiz2(x, y) {
//   if (x.includes(y)) {
//     return `${x}에는(은) ${y}가 포함되어 있습니다`;
//   } else {
//     return `${x}에는(은) ${y}가 포함되어있지 않습니다`;
//   }
// }

// function quiz3(email) {
//   if (email.includes("@")) {
//     return `${email}은 올바른 이메일 형식입니다`;
//   } else {
//     return `${email}은 올바르지않은 이메일 형식입니다`;
//   }
// }
