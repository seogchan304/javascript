// 참조: Array, Object, Function, Date, Math
const a = new Date(); // a는 데이트 타입

window.console.log(a);
window.console.log(a.getDate);
window.console.log(a.toLocaleString()); // 현재 사는 지역 기준으로 날짜 나타냄
window.console.log(a.toLocaleDateString());
window.console.log(a.toLocaleTimeString());

const b = Math.PI;
